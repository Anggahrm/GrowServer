JWT_SECRET=SuperSecretDoNotShareToAnyoneElse
DISCORD_BOT_TOKEN=Tokxxxxxxxen
# postgresql://[user[:password]@][host][:port][/dbname][?options]
DATABASE_URL=postgresql://growserver:ilovereimu@localhost:5432/growserver
# REDIS_URL=redis://localhost:6379

# Cloud deployment configuration (Heroku, Railway, Render, Fly.io, etc.)
# PORT is automatically set by most cloud platforms for the web frontend
# PORT=8080

# Server configuration overrides (optional - defaults from config.toml)
# WEB_ADDRESS=127.0.0.1
# WEB_PORT=443
# WEB_FRONTEND_PORT=8080
# SERVER_PORTS=17091
# LOGIN_URL=login.growserver.app:8080
# CDN_URL=growserver-cache.netlify.app
# LOG_LEVEL=info
